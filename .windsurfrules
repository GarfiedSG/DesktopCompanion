# 桌面伴侶 — Windsurf 專案規則

你正在開發「桌面伴侶」桌面寵物專案，寵物名為晴兒。
這是一個 Python + PySide6 的桌面應用，支援動畫、互動、設置與擴展功能。

---

## 一、語言與回覆

1. 始終用繁體中文回覆。
2. 代碼註釋用繁體中文。
3. 變數、函式、類別命名用英文，遵循 Python 慣例。
4. 與用戶溝通時，簡潔直接，不要過度解釋。

---

## 二、開發原則

1. 嚴格遵循 `docs/` 下的需求文檔與步驟指南。
2. 一次只實現一個步驟，不要提前實現後續步驟。
3. 每個步驟完成後，列出修改的檔案與驗收方式。
4. 不要引入需求文檔未提到的依賴。
5. 代碼要完整可運行，不要省略、不要用 `...` 或 `pass` 佔位。
6. 如果代碼太長，拆成多個檔案，不要一個檔案塞所有東西。
7. 遇到需求不明確時，先問用戶，不要自行假設。
8. 不要擅自重構已完成的步驟，除非用戶明確要求。

---

## 三、路徑與編碼

1. 所有檔案用 UTF-8 編碼。
2. 圖片路徑使用 `Path("文檔") / "晴兒.png"`，不要寫死反斜杠。
3. 中文路徑「文檔」保持不變，不要改成 `assets` 或 `resources`。
4. 專案內所有相對路徑用 `pathlib.Path`，不要用字串拼接。
5. 打包後資源路徑用 `sys._MEIPASS` 處理。

---

## 四、架構約束

1. 所有聲音播放必須經過 `AudioManager`，不要在各處直接 `new QSoundEffect`。
2. 所有擴展功能做成獨立 `Service`，不要塞進寵物窗口。
3. 寵物窗口只負責顯示、動畫、拖拽、點擊。
4. 設置窗口只負責改設定、發信號，不直接操作寵物窗口。
5. `ConfigManager` 統一管理 `config.json` 讀寫。
6. `ServiceManager` 統一管理所有 `Service` 的啟動、停止、套用設定。
7. `AnimationStateMachine` 只管狀態與幀，不管窗口移動。
8. `BehaviorScheduler` 只管行為決策，不管動畫播放。
9. `IdleScheduler` 只管待機動畫的隨機切換。
10. `IdleImporter` 只管導入動畫，不管播放。

---

## 五、技術棧

1. Python 3.11+、PySide6、JSON、PyInstaller。
2. 不引入 PyQt5、tkinter、wxPython 等其他 GUI 框架。
3. 不引入需求文檔未提到的第三方庫。
4. 可用的第三方庫：
   - `PySide6`：GUI 框架
   - `edge-tts`：粵語 TTS
   - `Pillow`：圖片處理
   - `urllib`：Gemini API 呼叫（標準庫）
5. 不要引入 `requests`、`aiohttp` 等 HTTP 庫，用標準庫 `urllib`。

---

## 六、動畫與聲音

1. 不同待機動畫切換用交叉淡化 0.2–0.4 秒。
2. 相同待機動畫切換不淡化，直接跳過。
3. 交叉淡化用兩個 `QLabel` 疊放 + `QGraphicsOpacityEffect`。
4. 音量固定 `DEFAULT_VOLUME = 0.8`，不設音量調節。
5. 全局靜音優先級最高，但不清空分類開關狀態。
6. 短音效用 `QSoundEffect`，長音頻/TTS 用 `QMediaPlayer`。
7. `QSoundEffect` 必須用 dict 快取，不要每次播放都新建物件。
8. 動畫 FPS：Idle 15、Walk 30、Click 30、Drag 60。
9. 視窗不可見時停止渲染。

---

## 七、待機動畫

1. 待機動畫放外部資料夾 `文檔/animations/idle/<動畫名>/`。
2. 幀檔名固定為 `frame_000.png`、`frame_001.png`、…（三位數補零）。
3. 導入時自然排序（`1, 2, 10` 而非 `1, 10, 2`）。
4. 導入時統一轉為 RGBA PNG，保留透明通道。
5. 導入時統一尺寸，建議最長邊 512–1024 px。
6. 新增動畫不需要重新編譯。
7. 相同動畫不連續切換。
8. 只有一個啟用動畫時不切換。
9. 切換間隔由 `min_interval` 與 `max_interval` 控制，下限 10 秒。

---

## 八、配置與緩存

1. `config.json` 存設定，啟動時讀取，變更時寫入。
2. 打包後 `config.json` 寫到 `Path.home() / ".desktop_pet"`，不要寫在程式目錄。
3. `greeting_cache.json` 按日期緩存問候，保留最近 7 天。
4. 緩存清理在程式啟動時執行一次。
5. JSON 解析失敗時直接重置，不要讓程式崩潰。
6. 所有 JSON 寫入用 `ensure_ascii=False, indent=2`。

---

## 九、粵語問候

1. 問候格式固定：`你好，今日係{日期}，{星期}。{治愈溫馨句子}`。
2. 觸發條件：每日首次啟動後 5 分鐘，當日不重複。
3. 聯網生成用 Gemini 免費層，模型 `gemini-2.5-flash`。
4. API Key 不硬編碼，存 `config.json`。
5. 聯網失敗或超時（10 秒）時回退本地句子庫。
6. 「是否聯網生成問候」開關關閉時，不發任何網絡請求。
7. TTS 用 `edge-tts`，粵語聲音 `zh-HK-HiuMaanNeural`。
8. 全局靜音時不播 TTS，但氣泡/通知仍顯示。
9. 氣泡顯示在晴兒旁邊，3–5 秒後淡出。

---

## 十、定時關機

1. 用 `QTimer` 每 30 秒檢查一次。
2. 倒計時預設 60 秒，可取消。
3. Windows：`shutdown /s /t 60`；取消：`shutdown /a`。
4. macOS / Linux：對應 `shutdown` 命令。
5. 測試時把 `_do_shutdown` 改成 `print`，確認邏輯正確再啟用真實命令。
6. 托盤通知提示用戶。

---

## 十一、動態壁紙

1. Windows 優先支援動態壁紙，用 `WorkerW` 嵌入 `QMediaPlayer`。
2. macOS / Linux 先降級為靜態壁紙或幻燈片。
3. 靜態壁紙：Windows 用 `SystemParametersInfoW`，macOS 用 `osascript`，Linux 用 `gsettings`。
4. 視頻靜音選項生效。
5. 支援循環播放。
6. 多螢幕策略要明確。

---

## 十二、程式碼風格

1. 縮排用 4 個空格，不用 Tab。
2. 行長不超過 100 字元。
3. 函式與類別之間空兩行。
4. import 分三組：標準庫、第三方、本地模組，組間空一行。
5. 類別名用 `PascalCase`，函式與變數用 `snake_case`，常數用 `UPPER_CASE`。
6. 私有方法與屬性用單下劃線前綴 `_`。
7. 信號用 `Signal`，不要用 `pyqtSignal`。
8. 用 `Path` 而非 `os.path`。
9. 用 f-string 而非 `%` 或 `.format()`。
10. 例外處理要具體，不要用裸 `except:`。

---

## 十三、PySide6 特定

1. 窗口 flags 用 `Qt.FramelessWindowHint | Qt.WindowStaysOnTopHint | Qt.Tool`。
2. 透明背景用 `WA_TranslucentBackground` + `WA_NoSystemBackground`。
3. `QApplication.setQuitOnLastWindowClosed(False)`，托盤程式不因關窗退出。
4. 用 `QTimer` 而非 `threading.Timer`。
5. 用 `Signal` / `Slot` 而非直接跨執行緒呼叫。
6. `QPropertyAnimation` 與 `QParallelAnimationGroup` 要用實例變數保存，避免被 GC 回收。
7. `QSystemTrayIcon` 的圖示要檢查檔案存在，否則用系統標準圖示。
8. 多螢幕用 `QGuiApplication.screenAt()` 與 `screen.availableGeometry()`。

---

## 十四、測試與驗收

1. 每個步驟完成後，自己運行並逐項檢查驗收標準。
2. 不要讓 Windsurf 連續做多個步驟。
3. 每一步完成後 `git commit` 一次。
4. 空閒 CPU 應低於 3%。
5. 記憶體穩定，不持續增長。
6. 退出後無殘留進程。
7. 多螢幕、高 DPI 環境要測試。
8. 動態壁紙在非 Windows 平台降級正常。

---

## 十五、禁止事項

1. 不要硬編碼 API Key、路徑、URL。
2. 不要引入需求外的功能。
3. 不要提前實現後續步驟。
4. 不要把擴展功能塞進寵物窗口。
5. 不要在各處直接 `new QSoundEffect`。
6. 不要用 `requests` 或 `aiohttp`。
7. 不要用 PyQt5 語法。
8. 不要寫死反斜杠路徑。
9. 不要在打包後把 config 寫在程式目錄。
10. 不要留 `print` 在正式代碼中，用 logging。

---

## 十六、文檔參考

開發時參考以下文件（按優先級）：

1. `docs/需求文檔.md`：完整需求，凍結範圍。
2. `docs/step1_骨架與窗口.md`：第 1 步實施指南。
3. `docs/step2_動畫與行為.md`：第 2 步實施指南。
4. `docs/step3_設置與聲音.md`：第 3 步實施指南。
5. `docs/step4_服務擴展.md`：第 4 步實施指南。
6. `docs/step5_優化打包.md`：第 5 步實施指南。
7. `docs/給Windsurf的提示詞.md`：用戶操作指南。

---

## 十七、當前任務判斷

當用戶說「開始第 N 步」或類似指令時：

1. 先讀取對應的 `docs/stepN_*.md`。
2. 只實現該步驟的內容，不要提前。
3. 完成後列出修改的檔案與驗收方式。
4. 等待用戶確認後，再進入下一步。

當用戶說「繼續」時：

1. 檢查當前步驟是否完成。
2. 若未完成，繼續完成當前步驟。
3. 若已完成，詢問是否進入下一步。

當用戶說「修改」時：

1. 先確認要修改的具體內容。
2. 只改動相關部分，不要大規模重構。
3. 完成後列出修改的檔案與影響範圍。

---

## 十八、最終提醒

- 晴兒核心只負責顯示、動畫、拖拽、點擊。
- 所有擴展做成獨立 Service。
- 動畫素材放外部資料夾，用戶新增動畫無需重新編譯。
- 交叉淡化讓不同動畫切換自然；相同動畫不淡化。
- 長時間循環動畫首尾幀盡量一致。
- 聲音默認靜音或低調，全局靜音優先，不設音量調節。
- 問候觸發：首次啟動後 5 分鐘，當日不重複。
- 聯網用 Gemini 免費層，失敗回退本地。
- 緩存保留最近 7 天。
- 動態壁紙最難，Windows 優先，其他平台降級。

---

版本：v1.0
對應需求：桌面伴侶需求文檔 v2.1
最後更新：2026-09-10